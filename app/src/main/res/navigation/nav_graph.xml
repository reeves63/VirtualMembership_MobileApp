<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    app:startDestination="@id/homeFragment">

    <!-- Home Fragment -->
    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.uts_pagisore.HomeFragment"
        android:label="HomeFragment"
        tools:layout="@layout/fragment_home" />

    <!-- QR Fragment -->
    <fragment
        android:id="@+id/qrFragment"
        android:name="com.example.uts_pagisore.QrFragment"
        android:label="QrFragment"
        tools:layout="@layout/fragment_qr" />

    <!-- Messages Fragment -->
    <fragment
        android:id="@+id/messagesFragment"
        android:name="com.example.uts_pagisore.MessagesFragment"
        android:label="MessagesFragment"
        tools:layout="@layout/fragment_messages" />

    <!-- Profile Fragment -->
    <fragment
        android:id="@+id/profileFragment"
        android:name="com.example.uts_pagisore.ProfileFragment"
        android:label="ProfileFragment"
        tools:layout="@layout/fragment_profile" />

</navigation>
